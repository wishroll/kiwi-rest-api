version: 1.0
runtime: nodejs14
build:
  commands:    
    build:
      - npm install --production                                  
run:                              
  command: npm start
  env: 
    - name: NODE_ENV
      value: production