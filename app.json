{
  "environments": {
    "test": {
      "scripts": {
        "test-setup": "npm install && npm migrate",
        "test": "npm test"
      },
      "buildpacks": [{ "url": "heroku/nodejs" }],
      "addons": ["heroku-postgresql:in-dyno"]
    }
  }
}
